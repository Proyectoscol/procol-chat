# Asterisk ARI (local en el VPS-2)
ARI_URL=http://127.0.0.1:8088
ARI_USERNAME=stasis
ARI_PASSWORD=changeme
ARI_APP=procol-routing

# Rails (VPS-1) — endpoints internos
RAILS_BASE_URL=https://chat.tudominio.com
RAILS_SIP_SECRET=__mismo_valor_que_ASTERISK_ROUTING_SECRET_en_Rails__

# Routing
DIAL_TIMEOUT_SECONDS=15
IVR_PROMPT=ivr-ciudades        # nombre del audio en FreePBX (sin extensión)
FALLBACK_CONTEXT=from-voicemail # destino si Rails no responde
